# Remove all versions and use latest

# Core FastAPI and web framework
fastapi
uvicorn[standard]
starlette
pydantic
pydantic-settings

# Database
SQLAlchemy
alembic
PyMySQL
mysql-connector-python

# Authentication and Security
passlib[bcrypt]
PyJWT[crypto]
pyotp
qrcode[pil]
email-validator
cryptography

# HTTP Client
httpx
requests

# Utilities
python-dotenv
python-multipart
Pillow

# Development and Testing
pytest
httpx
faker

# Telemetry (OpenTelemetry without problematic dependencies)
opentelemetry-api
opentelemetry-sdk
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-sqlalchemy
opentelemetry-exporter-jaeger-thrift

# JSON handling (without Rust dependency)
# Using standard json instead of orjson to avoid Rust
# orjson==3.9.10  # Commented out - has Rust dependency

# Other utilities
coloredlogs
click
Jinja2
MarkupSafe
packaging
setuptools
wheel

# Avoid these packages with Rust dependencies:
# - rpds-py (Rust dependency)
# - orjson (Rust dependency)
# - watchfiles (Rust dependency) - using alternative
# - pydantic-core > 2.14 (may have Rust issues)

# Alternative to watchfiles for development
# Use uvicorn's built-in reloader instead
